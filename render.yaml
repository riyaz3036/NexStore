services:
  - type: web
    name: nexstore-server
    env: docker
    repo: https://github.com/yourusername/NexStore
    dockerfilePath: Dockerfile
    buildFilter:
      paths:
        - server/**/*
    autoDeploy: true
